<UserControl x:Class="EventPlanner.Views.FinancePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Finance" Style="{StaticResource PageTitle}"/>

        <UniformGrid Grid.Row="1" Columns="5" Margin="0,0,0,14">
            <Border Style="{StaticResource Card}" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Expected income" Style="{StaticResource CardLabel}"/>
                    <TextBlock Text="{Binding ExpectedIncome, StringFormat={}{0:0.00} €}" Style="{StaticResource CardValue}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Paid income" Style="{StaticResource CardLabel}"/>
                    <TextBlock Text="{Binding PaidIncome, StringFormat={}{0:0.00} €}" Style="{StaticResource CardValue}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Outstanding" Style="{StaticResource CardLabel}"/>
                    <TextBlock Text="{Binding Outstanding, StringFormat={}{0:0.00} €}" Style="{StaticResource CardValue}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Expenses" Style="{StaticResource CardLabel}"/>
                    <TextBlock Text="{Binding TotalExpenses, StringFormat={}{0:0.00} €}" Style="{StaticResource CardValue}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="Profit" Style="{StaticResource CardLabel}"/>
                    <TextBlock Text="{Binding Profit, StringFormat={}{0:0.00} €}" Style="{StaticResource CardValue}"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <DataGrid x:Name="ExpensesGrid" Grid.Row="2" ItemsSource="{Binding Expenses}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ExpenseId}" Width="60"/>
                <DataGridTextColumn Header="Event ID" Binding="{Binding EventId}" Width="80"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="*"/>
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat={}{0:0.00} €}" Width="120"/>
                <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="240"/>
                <DataGridTextColumn Header="Paid at" Binding="{Binding PaidAt, StringFormat=\{0:dd.MM.yyyy\}}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,14,0,0">
            <Button Content="Add Expense" Style="{StaticResource PrimaryButton}" Click="AddExpense_Click"/>
            <Button Content="Edit" Style="{StaticResource SecondaryButton}" Click="EditExpense_Click"/>
            <Button Content="Delete" Style="{StaticResource DangerButton}" Click="DeleteExpense_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
